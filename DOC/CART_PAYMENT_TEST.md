# 购物车合并支付功能测试清单

## 前置条件

1. **数据库迁移**
   - [ ] 执行 `database_update_cart_batch.sql` 脚本，为 `orders.trade_no` 添加索引
   ```sql
   USE bookmusic_mall;
   ALTER TABLE orders ADD INDEX idx_trade_no (trade_no);
   ```

2. **支付宝配置**
   - [ ] 确认 `.env` 文件中已配置支付宝参数：
     - `ALIPAY_APP_ID`
     - `ALIPAY_PRIVATE_KEY`
     - `ALIPAY_PUBLIC_KEY`
     - `ALIPAY_NOTIFY_URL`

3. **服务器环境**
   - [ ] 确保 Apache/Nginx 已启动
   - [ ] 确保 PHP 8.2+ 和 MySQL 8.0+ 运行正常

---

## 测试步骤

### 1. 购物车基础功能测试

#### 1.1 添加商品到购物车
- [ ] 访问商品详情页（小说或音乐）
- [ ] 点击"加入购物车"按钮
- [ ] 验证是否显示成功通知："已加入购物车"
- [ ] 验证导航栏购物车数量是否更新

#### 1.2 查看购物车
- [ ] 访问 `/cart` 页面
- [ ] 验证购物车商品列表正确显示
- [ ] 验证商品信息（标题、类型、价格、封面图）正确
- [ ] 验证全选复选框默认选中

#### 1.3 全选/取消全选
- [ ] 点击顶部"全选"复选框
- [ ] 验证所有商品复选框是否同步取消
- [ ] 再次点击"全选"
- [ ] 验证所有商品复选框是否同步选中
- [ ] 手动取消某个商品的复选框
- [ ] 验证顶部的"全选"复选框是否自动取消

#### 1.4 移除单个商品
- [ ] 点击某商品的"移除"按钮
- [ ] 验证确认对话框弹出
- [ ] 点击"确定"
- [ ] 验证该行商品以动画形式消失
- [ ] 验证总价和商品数量自动更新
- [ ] 如果购物车为空，验证是否跳转到空状态页面

#### 1.5 清空购物车
- [ ] 点击"清空购物车"按钮
- [ ] 验证确认对话框弹出
- [ ] 点击"确定"
- [ ] 验证购物车被清空，显示空状态页面

---

### 2. 合并支付流程测试

#### 2.1 选择商品并结算
- [ ] 在购物车中选择 2-3 件商品
- [ ] 验证"已选 X 件商品"和"合计金额"正确显示
- [ ] 点击"合并结算"按钮
- [ ] 验证按钮变为禁用状态，显示"正在创建订单..."

#### 2.2 支付宝扫码模态框
- [ ] 验证模态框正确弹出
- [ ] 验证待支付金额显示正确
- [ ] 验证支付宝二维码图片加载成功
- [ ] 验证状态文字显示："等待扫码支付..."

#### 2.3 支付状态轮询
- [ ] 打开浏览器开发者工具（F12）
- [ ] 切换到 Network（网络）标签
- [ ] 观察是否有每 3 秒一次的 `/api/order/status` 请求
- [ ] 验证请求参数中包含正确的 `batch_trade_no`

#### 2.4 模拟支付成功
- [ ] 使用支付宝沙箱或真实账号扫描二维码
- [ ] 完成支付流程
- [ ] 观察模态框中的状态是否在 3-5 秒内变为绿色对勾："支付成功！正在跳转..."
- [ ] 验证 1.5 秒后自动跳转到 `/user/downloads` 页面

#### 2.5 手动关闭模态框
- [ ] 在支付完成前，点击模态框右上角的 "×" 按钮
- [ ] 验证模态框关闭
- [ ] 验证浏览器不再发送状态查询请求
- [ ] 验证"合并结算"按钮恢复可用状态

---

### 3. 后端 API 测试

#### 3.1 购物车结算接口
```bash
POST /api/checkout/cart
Content-Type: application/x-www-form-urlencoded

product_ids[]=1&product_ids[]=2&product_ids[]=3
```

**预期响应：**
```json
{
  "success": true,
  "batch_trade_no": "BATCH_20260605123456_7890",
  "total_amount": "99.00",
  "pay_url": "https://openapi.alipay.com/...",
  "item_count": 3
}
```

#### 3.2 批次订单状态查询接口
```bash
GET /api/order/status?batch_trade_no=BATCH_20260605123456_7890
```

**预期响应（待支付）：**
```json
{
  "success": true,
  "status": "pending",
  "batch_trade_no": "BATCH_20260605123456_7890",
  "order_count": 3,
  "message": "等待支付"
}
```

**预期响应（支付成功）：**
```json
{
  "success": true,
  "status": "paid",
  "batch_trade_no": "BATCH_20260605123456_7890",
  "order_count": 3,
  "message": "支付成功"
}
```

#### 3.3 移除购物车商品接口
```bash
POST /api/cart/remove
Content-Type: application/x-www-form-urlencoded

product_id=1
```

**预期响应：**
```json
{
  "success": true,
  "message": "已移除",
  "cart_count": 2
}
```

---

### 4. 数据库验证

#### 4.1 检查批次订单创建
```sql
SELECT id, order_no, trade_no, status, price 
FROM orders 
WHERE trade_no LIKE 'BATCH_%' 
ORDER BY id DESC 
LIMIT 10;
```

**验证点：**
- [ ] 同一批次的订单具有相同的 `trade_no`（批次流水号）
- [ ] 订单状态初始为 `pending`
- [ ] 支付成功后状态变为 `paid`

#### 4.2 检查购物车清空
```sql
SELECT * FROM cart WHERE user_id = 1;
```

**验证点：**
- [ ] 结算后的商品已从购物车中删除

#### 4.3 检查索引性能
```sql
EXPLAIN SELECT id, status, pay_time 
FROM orders 
WHERE trade_no = 'BATCH_20260605123456_7890' AND user_id = 1;
```

**验证点：**
- [ ] 查询计划中使用 `idx_trade_no` 索引
- [ ] `type` 字段值为 `ref` 或 `range`，而非 `ALL`（全表扫描）

---

### 5. 异常场景测试

#### 5.1 网络异常
- [ ] 断开网络连接后点击"合并结算"
- [ ] 验证是否显示"网络错误"提示
- [ ] 验证按钮是否恢复可用状态

#### 5.2 重复提交
- [ ] 快速连续点击"合并结算"按钮多次
- [ ] 验证防连击机制是否生效（只发起一次请求）

#### 5.3 空购物车结算
- [ ] 不选择任何商品，直接点击"合并结算"
- [ ] 验证是否显示"请至少选择一件商品"提示

#### 5.4 支付超时
- [ ] 生成订单后等待 15 分钟不支付
- [ ] 尝试重新支付该批次订单
- [ ] 验证系统是否正确处理超时订单

---

## 常见问题排查

### 问题 1：二维码无法加载
**可能原因：**
- 支付宝 SDK 配置错误
- `.env` 文件中的参数未正确读取

**解决方法：**
1. 检查 `includes/AlipaySDK.php` 中的配置
2. 验证 `.env` 文件中的支付宝参数
3. 查看 `logs/error.log` 中的错误日志

### 问题 2：支付状态轮询失败
**可能原因：**
- 路由配置错误
- 中间件鉴权失败

**解决方法：**
1. 检查 `public/index.php` 中的路由配置
2. 确认用户已登录
3. 浏览器控制台查看网络请求的详细错误

### 问题 3：支付成功后未跳转
**可能原因：**
- 前端 JavaScript 错误
- 批次订单状态查询返回格式不正确

**解决方法：**
1. 打开浏览器控制台（F12），查看 Console 标签的错误
2. 检查 `/api/order/status` 接口的返回数据格式
3. 确认数据库中订单状态已更新为 `paid`

---

## 性能优化建议

1. **数据库索引**
   - 确保 `orders.trade_no` 字段有索引（已通过 `database_update_cart_batch.sql` 添加）
   - 定期分析慢查询日志

2. **前端优化**
   - 考虑将轮询间隔从 3 秒调整为 5 秒（减少服务器压力）
   - 使用 WebSocket 代替轮询（实时性更好）

3. **安全性**
   - 增加 CSRF Token 验证
   - 限制同一用户的并发结算请求
   - 添加支付频率限制（防止恶意刷单）

---

## 验收标准

- [ ] 所有测试用例通过
- [ ] 无 JavaScript 控制台错误
- [ ] 无 PHP 错误日志
- [ ] 数据库查询性能符合预期（使用索引）
- [ ] 移动端响应式布局正常
- [ ] 支付流程完整闭环（从加购到下载）

---

**测试人员：** ______________  
**测试日期：** ______________  
**测试结果：** ☐ 通过  ☐ 失败  

**备注：**
_______________________________________________________
_______________________________________________________
