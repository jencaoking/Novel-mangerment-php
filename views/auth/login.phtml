<?php require __DIR__ . '/../_header.phtml'; ?>

<section class="py-5" style="background: linear-gradient(180deg, var(--color-gray-50) 0%, var(--color-white) 100%); min-height: calc(100vh - 72px);">
    <div class="container">
        <div class="row justify-content-center align-items-center" style="min-height: calc(100vh - 200px);">
            <div class="col-lg-5 col-md-7 col-sm-10">
                <div class="card border-0 shadow-lg" style="border-radius: var(--radius-xl);">
                    <div class="card-body p-5">
                        <div class="text-center mb-4">
                            <div class="mb-3" style="width: 80px; height: 80px; margin: 0 auto; background: var(--color-primary-gradient); border-radius: var(--radius-xl); display: flex; align-items: center; justify-content: center;">
                                <i class="bi bi-person text-white" style="font-size: 2.5rem;"></i>
                            </div>
                            <h2 class="fw-bold">欢迎回来</h2>
                            <p class="text-gray-600 mb-0">登录您的 BookMusic Mall 账户</p>
                        </div>
                        
                        <?php if (isset($error) && $error): ?>
                            <div class="alert alert-danger d-flex align-items-center gap-2 mb-4" role="alert">
                                <i class="bi bi-exclamation-circle-fill"></i>
                                <span><?= e($error) ?></span>
                            </div>
                        <?php endif; ?>
                        
                        <form method="POST" action="/login">
                            <input type="hidden" name="csrf_token" value="<?= generateCSRFToken() ?>">
                            
                            <div class="mb-4">
                                <label for="username" class="form-label fw-semibold">用户名 / 邮箱</label>
                                <input type="text" id="username" name="username" required 
                                       class="form-control form-control-lg"
                                       placeholder="请输入用户名或邮箱"
                                       value="<?= e($_POST['username'] ?? '') ?>">
                            </div>
                            
                            <div class="mb-4">
                                <label for="password" class="form-label fw-semibold">密码</label>
                                <input type="password" id="password" name="password" required 
                                       class="form-control form-control-lg"
                                       placeholder="请输入密码">
                            </div>
                            
                            <div class="mb-4">
                                <label for="captcha" class="form-label fw-semibold">验证码</label>
                                <div class="d-flex gap-3">
                                    <input type="text" name="captcha" id="captcha" required 
                                           class="form-control form-control-lg"
                                           placeholder="请输入验证码">
                                    <img src="/captcha" alt="验证码" 
                                         class="rounded cursor-pointer border"
                                         style="width: 120px; height: 46px;"
                                         onclick="this.src='/captcha?'+Math.random()"
                                         title="点击刷新验证码">
                                </div>
                            </div>
                            
                            <div class="mb-4 form-check">
                                <input type="checkbox" class="form-check-input" id="remember" name="remember" 
                                       <?= isset($_POST['remember']) ? 'checked' : '' ?>>
                                <label class="form-check-label" for="remember">记住我（7天内自动登录）</label>
                            </div>
                            
                            <button type="submit" class="btn btn-primary btn-lg w-100">
                                <i class="bi bi-box-arrow-in-right me-2"></i> 登录
                            </button>
                        </form>
                        
                        <div class="text-center mt-4 pt-4" style="border-top: 1px solid var(--color-gray-200);">
                            <p class="mb-0">还没有账户？ <a href="/register" class="fw-semibold" style="color: var(--color-primary);">立即注册</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
.form-control-lg {
    padding: 0.875rem 1rem;
    border: 2px solid var(--color-gray-200);
    border-radius: var(--radius-lg);
    font-size: 1rem;
}
.form-control-lg:focus {
    border-color: var(--color-primary);
    box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1);
}
</style>

<?php require __DIR__ . '/../_footer.phtml'; ?>
