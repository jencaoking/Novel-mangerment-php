<?php require __DIR__ . '/../_header.phtml'; ?>

<section class="py-5" style="background: var(--color-bg-muted); min-height: calc(100dvh - var(--header-height));">
    <div class="container">
        <div class="row justify-content-center align-items-center" style="min-height: calc(100dvh - 200px);">
            <div class="col-lg-5 col-md-7 col-sm-10">
                <div class="card border-0 shadow-lg">
                    <div class="card-body p-5">
                        <div class="text-center mb-4">
                            <div class="mb-3" style="width: 72px; height: 72px; margin: 0 auto; background: var(--color-primary); border-radius: var(--radius-xl); display: flex; align-items: center; justify-content: center;">
                                <i class="bi bi-person-plus" style="font-size: 2rem; color: var(--color-text-inverse);"></i>
                            </div>
                            <h2 class="fw-bold">创建账户</h2>
                            <p style="color: var(--color-text-muted); margin-bottom: 0;">加入 BookMusic Mall，开启您的数字内容之旅</p>
                        </div>
                        
                        <?php if (isset($error) && $error): ?>
                            <div class="alert alert-danger d-flex align-items-center gap-2 mb-4" role="alert">
                                <i class="bi bi-exclamation-circle-fill"></i>
                                <span><?= e($error) ?></span>
                            </div>
                        <?php endif; ?>
                        
                        <?php if (isset($success) && $success): ?>
                            <div class="alert alert-success d-flex align-items-center gap-2 mb-4" role="alert">
                                <i class="bi bi-check-circle-fill"></i>
                                <span><?= e($success) ?></span>
                            </div>
                            <script>
                                setTimeout(function() { window.location.href = '/login'; }, 3000);
                                // 页面加载完成后禁用表单
                                document.addEventListener('DOMContentLoaded', function() {
                                    const form = document.getElementById('registerForm');
                                    if (form) {
                                        form.disabled = true;
                                        const inputs = form.querySelectorAll('input, button');
                                        inputs.forEach(input => input.disabled = true);
                                        const submitBtn = form.querySelector('button[type="submit"]');
                                        if (submitBtn) {
                                            submitBtn.innerHTML = '<i class="bi bi-check-circle me-2"></i> 注册成功';
                                            submitBtn.classList.remove('btn-primary');
                                            submitBtn.classList.add('btn-success');
                                        }
                                    }
                                });
                            </script>
                        <?php endif; ?>
                        
                        <form method="POST" action="/register" id="registerForm">
                            <input type="hidden" name="csrf_token" value="<?= generateCSRFToken() ?>">
                            
                            <div class="mb-4">
                                <label for="username" class="form-label fw-semibold">用户名</label>
                                <input type="text" id="username" name="username" required 
                                       class="form-control form-control-lg"
                                       placeholder="3-20个字符，支持中文、英文、数字"
                                       value="<?= e($_POST['username'] ?? '') ?>">
                            </div>
                            
                            <div class="mb-4">
                                <label for="email" class="form-label fw-semibold">邮箱地址</label>
                                <input type="email" id="email" name="email" required 
                                       class="form-control form-control-lg"
                                       placeholder="your@email.com"
                                       value="<?= e($_POST['email'] ?? '') ?>">
                            </div>
                            
                            <div class="mb-4">
                                <label for="password" class="form-label fw-semibold">密码</label>
                                <input type="password" id="password" name="password" required 
                                       class="form-control form-control-lg"
                                       placeholder="至少6个字符" data-min-length="6">
                            </div>
                            
                            <div class="mb-4">
                                <label for="confirm_password" class="form-label fw-semibold">确认密码</label>
                                <input type="password" id="confirm_password" name="confirm_password" required 
                                       class="form-control form-control-lg"
                                       placeholder="再次输入密码">
                            </div>
                            
                            <div class="mb-4">
                                <label for="captcha" class="form-label fw-semibold">验证码</label>
                                <div class="d-flex gap-3">
                                    <input type="text" name="captcha" id="captcha" required 
                                           class="form-control form-control-lg"
                                           placeholder="请输入验证码">
                                    <img src="/captcha" alt="验证码" 
                                         class="rounded cursor-pointer border"
                                         style="width: 120px; height: 46px;"
                                         onclick="this.src='/captcha?'+Math.random()"
                                         title="点击刷新验证码">
                                </div>
                            </div>
                            
                            <button type="submit" class="btn btn-primary btn-lg w-100" id="submitBtn">
                                <i class="bi bi-person-plus me-2"></i> 创建账户
                            </button>
                        </form>
                        
                        <div class="text-center mt-4 pt-4" style="border-top: 1px solid var(--color-gray-200);">
                            <p class="mb-0">已有账户？ <a href="/login" class="fw-semibold" style="color: var(--color-primary);">立即登录</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
.form-control-lg {
    padding: 0.875rem 1rem;
    border: 2px solid var(--color-gray-200);
    border-radius: var(--radius-lg);
    font-size: 1rem;
}
.form-control-lg:focus {
    border-color: var(--color-primary);
    box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1);
}
</style>

<?php require __DIR__ . '/../_footer.phtml'; ?>