<?php require __DIR__ . '/_header.phtml'; ?>

<section class="products-page">
    <div class="container">
        <!-- 页面标题区 -->
        <div class="page-header-section">
            <h1 class="page-title">
                <i class="bi bi-book-heart"></i> 小说商城
            </h1>
            <p class="page-subtitle">精选优质文学作品，开启您的阅读之旅</p>
        </div>

        <!-- 搜索和筛选区 -->
        <div class="filter-toolbar">
            <form method="GET" class="search-form">
                <div class="search-input-wrapper">
                    <i class="bi bi-search"></i>
                    <input type="text" name="search" placeholder="搜索小说名称或作者..." 
                           value="<?= e($search) ?>" class="search-input">
                </div>
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-search"></i> 搜索
                </button>
            </form>

            <div class="filter-options">
                <div class="filter-select-wrapper">
                    <i class="bi bi-funnel"></i>
                    <select name="category" onchange="this.form.submit()" class="filter-select">
                        <option value="0">全部分类</option>
                        <?php foreach ($categories as $cat): ?>
                            <option value="<?= $cat['id'] ?>" <?= $category == $cat['id'] ? 'selected' : '' ?>>
                                <?= e($cat['name']) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </div>

                <div class="filter-select-wrapper">
                    <i class="bi bi-sort-down-alt"></i>
                    <select name="sort" onchange="this.form.submit()" class="filter-select">
                        <option value="latest" <?= $sort === 'latest' ? 'selected' : '' ?>>最新上架</option>
                        <option value="hot" <?= $sort === 'hot' ? 'selected' : '' ?>>最热门</option>
                        <option value="price_asc" <?= $sort === 'price_asc' ? 'selected' : '' ?>>价格从低到高</option>
                        <option value="price_desc" <?= $sort === 'price_desc' ? 'selected' : '' ?>>价格从高到低</option>
                    </select>
                </div>
            </div>
        </div>

        <!-- 产品网格 -->
        <?php if (empty($novels)): ?>
            <div class="empty-state">
                <i class="bi bi-book-x"></i>
                <h3>暂无小说</h3>
                <p>没有找到符合条件的小说，请尝试其他搜索条件</p>
                <a href="/novels" class="btn btn-primary">
                    <i class="bi bi-arrow-counterclockwise"></i> 查看全部
                </a>
            </div>
        <?php else: ?>
            <div class="products-grid">
                <?php foreach ($novels as $index => $novel): ?>
                    <div class="product-card animate-ready" style="animation-delay: <?= $index * 0.05 ?>s">
                        <div class="product-image-wrapper">
                            <img src="/uploads/cover/<?= e($novel['cover']) ?>" 
                                 alt="<?= e($novel['title']) ?>" 
                                 class="product-image"
                                 loading="lazy">
                            <div class="product-hover-overlay">
                                <a href="/product/<?= $novel['id'] ?>" class="btn btn-outline btn-sm">
                                    <i class="bi bi-eye"></i> 查看详情
                                </a>
                            </div>
                            <?php if ($novel['sales'] > 100): ?>
                                <span class="hot-badge">
                                    <i class="bi bi-fire"></i> 热销
                                </span>
                            <?php endif; ?>
                        </div>
                        <div class="product-content">
                            <span class="product-category">
                                <i class="bi bi-tag"></i>
                                <?= e($novel['category_name'] ?? '小说') ?>
                            </span>
                            <h3 class="product-title"><?= e($novel['title']) ?></h3>
                            <p class="product-author">
                                <i class="bi bi-person"></i> <?= e($novel['author']) ?>
                            </p>
                            <div class="product-stats">
                                <div class="stat-mini">
                                    <i class="bi bi-cart"></i>
                                    <span><?= $novel['sales'] ?></span>
                                </div>
                                <div class="stat-mini">
                                    <i class="bi bi-star"></i>
                                    <span><?= number_format($novel['rating_avg'] ?? 0, 1) ?></span>
                                </div>
                            </div>
                            <div class="product-footer">
                                <span class="product-price">
                                    <span class="price-symbol">¥</span><?= number_format($novel['price'], 2) ?>
                                </span>
                                <button class="btn btn-secondary btn-icon" onclick="event.stopPropagation(); addToCart(<?= $novel['id'] ?>)">
                                    <i class="bi bi-cart-plus"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>

            <!-- 分页 -->
            <?php if ($pagination['total_pages'] > 1): ?>
                <div class="pagination-container">
                    <nav class="pagination">
                        <?php if ($pagination['has_previous']): ?>
                            <a href="?page=<?= $page - 1 ?>&category=<?= $category ?>&sort=<?= $sort ?>&search=<?= urlencode($search) ?>" 
                               class="pagination-btn prev">
                                <i class="bi bi-chevron-left"></i> 上一页
                            </a>
                        <?php endif; ?>

                        <div class="pagination-numbers">
                            <?php 
                            $start = max(1, $page - 2);
                            $end = min($pagination['total_pages'], $page + 2);
                            if ($start > 1): ?>
                                <a href="?page=1&category=<?= $category ?>&sort=<?= $sort ?>&search=<?= urlencode($search) ?>" class="pagination-number">1</a>
                                <?php if ($start > 2): ?>
                                    <span class="pagination-ellipsis">...</span>
                                <?php endif; ?>
                            <?php endif; ?>

                            <?php for ($i = $start; $i <= $end; $i++): ?>
                                <a href="?page=<?= $i ?>&category=<?= $category ?>&sort=<?= $sort ?>&search=<?= urlencode($search) ?>" 
                                   class="pagination-number <?= $i == $page ? 'active' : '' ?>">
                                    <?= $i ?>
                                </a>
                            <?php endfor; ?>

                            <?php if ($end < $pagination['total_pages']): ?>
                                <?php if ($end < $pagination['total_pages'] - 1): ?>
                                    <span class="pagination-ellipsis">...</span>
                                <?php endif; ?>
                                <a href="?page=<?= $pagination['total_pages'] ?>&category=<?= $category ?>&sort=<?= $sort ?>&search=<?= urlencode($search) ?>" 
                                   class="pagination-number">
                                    <?= $pagination['total_pages'] ?>
                                </a>
                            <?php endif; ?>
                        </div>

                        <?php if ($pagination['has_next']): ?>
                            <a href="?page=<?= $page + 1 ?>&category=<?= $category ?>&sort=<?= $sort ?>&search=<?= urlencode($search) ?>" 
                               class="pagination-btn next">
                                下一页 <i class="bi bi-chevron-right"></i>
                            </a>
                        <?php endif; ?>
                    </nav>

                    <div class="pagination-info">
                        第 <strong><?= $page ?></strong> / <strong><?= $pagination['total_pages'] ?></strong> 页 
                        · 共 <strong><?= $pagination['total_items'] ?></strong> 件商品
                    </div>
                </div>
            <?php endif; ?>
        <?php endif; ?>
    </div>
</section>

<script src="/assets/js/main.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function() {
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver(function(entries) {
        entries.forEach(function(entry) {
            if (entry.isIntersecting) {
                entry.target.style.opacity = '1';
                entry.target.style.transform = 'translateY(0)';
                observer.unobserve(entry.target);
            }
        });
    }, observerOptions);

    document.querySelectorAll('.product-card').forEach(card => {
        card.style.opacity = '0';
        card.style.transform = 'translateY(30px)';
        card.style.transition = 'opacity 0.5s ease-out, transform 0.5s ease-out';
        observer.observe(card);
    });
});

async function addToCart(productId) {
    try {
        const response = await fetch('/api/cart/add', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: `product_id=${productId}`
        });
        const data = await response.json();
        
        if (data.success) {
            showNotification('已加入购物车', 'success');
            updateCartCount(data.cart_count);
        } else {
            showNotification(data.message || '加入失败', 'error');
        }
    } catch (err) {
        console.error(err);
        showNotification('网络错误，请稍后再试', 'error');
    }
}
</script>

<style>
.products-page {
    padding: 120px 0 60px;
    background: linear-gradient(180deg, var(--color-gray-50) 0%, var(--color-white) 100%);
}

.page-header-section {
    text-align: center;
    margin-bottom: 2.5rem;
}

.page-title {
    font-family: var(--font-display);
    font-size: 2.5rem;
    font-weight: 800;
    margin-bottom: 0.5rem;
    background: var(--color-primary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
}

.page-subtitle {
    color: var(--color-gray-600);
    font-size: 1.1rem;
}

.filter-toolbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 2.5rem;
    padding: 1.5rem;
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    flex-wrap: wrap;
}

.search-form {
    display: flex;
    gap: 0.75rem;
    flex: 1;
    max-width: 500px;
}

.search-input-wrapper {
    flex: 1;
    position: relative;
    display: flex;
    align-items: center;
}

.search-input-wrapper i {
    position: absolute;
    left: 1rem;
    color: var(--color-gray-400);
}

.search-input {
    width: 100%;
    padding: 0.8rem 1rem 0.8rem 2.5rem;
    border: 2px solid var(--color-gray-200);
    border-radius: var(--radius-lg);
    font-size: 1rem;
    font-family: var(--font-body);
    transition: all var(--transition-fast);
}

.search-input:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1);
}

.filter-options {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
}

.filter-select-wrapper {
    position: relative;
    display: flex;
    align-items: center;
}

.filter-select-wrapper i {
    position: absolute;
    left: 1rem;
    color: var(--color-gray-400);
    pointer-events: none;
}

.filter-select {
    padding: 0.8rem 1rem 0.8rem 2.5rem;
    border: 2px solid var(--color-gray-200);
    border-radius: var(--radius-lg);
    font-size: 0.95rem;
    font-family: var(--font-body);
    cursor: pointer;
    background: var(--color-white);
    transition: all var(--transition-fast);
}

.filter-select:focus {
    outline: none;
    border-color: var(--color-primary);
}

.products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 1.75rem;
    margin-bottom: 3rem;
}

.product-card {
    background: var(--color-white);
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-sm);
    transition: all var(--transition-base);
    cursor: pointer;
}

.product-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-xl);
}

.product-image-wrapper {
    position: relative;
    overflow: hidden;
    aspect-ratio: 3/4;
    background: var(--color-gray-100);
}

.product-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
}

.product-card:hover .product-image {
    transform: scale(1.08);
}

.product-hover-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.7), transparent 50%);
    opacity: 0;
    transition: opacity var(--transition-base);
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding: 1.5rem;
}

.product-card:hover .product-hover-overlay {
    opacity: 1;
}

.hot-badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    padding: 0.3rem 0.75rem;
    background: linear-gradient(135deg, #ff6b6b, #ee5a24);
    color: white;
    font-size: 0.75rem;
    font-weight: 700;
    border-radius: var(--radius-full);
    display: flex;
    align-items: center;
    gap: 0.3rem;
    box-shadow: 0 2px 8px rgba(238, 90, 36, 0.3);
}

.product-content {
    padding: 1.25rem;
}

.product-category {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.25rem 0.75rem;
    background: rgba(79, 70, 229, 0.1);
    color: var(--color-primary);
    font-size: 0.75rem;
    font-weight: 600;
    border-radius: var(--radius-full);
    margin-bottom: 0.5rem;
}

.product-title {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--color-gray-900);
    margin-bottom: 0.25rem;
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.product-author {
    color: var(--color-gray-500);
    font-size: 0.9rem;
    margin-bottom: 0.75rem;
    display: flex;
    align-items: center;
    gap: 0.3rem;
}

.product-stats {
    display: flex;
    gap: 1rem;
    margin-bottom: 0.75rem;
    padding-bottom: 0.75rem;
    border-bottom: 1px solid var(--color-gray-100);
}

.stat-mini {
    display: flex;
    align-items: center;
    gap: 0.3rem;
    color: var(--color-gray-500);
    font-size: 0.85rem;
}

.stat-mini i {
    color: var(--color-primary);
}

.product-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.product-price {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--color-primary);
}

.price-symbol {
    font-size: 1rem;
}

.btn-icon {
    padding: 0.6rem 0.8rem;
}

.empty-state {
    text-align: center;
    padding: 4rem 2rem;
    color: var(--color-gray-500);
}

.empty-state i {
    font-size: 5rem;
    margin-bottom: 1.5rem;
    opacity: 0.3;
}

.empty-state h3 {
    font-size: 1.5rem;
    margin-bottom: 0.75rem;
    color: var(--color-gray-700);
}

.empty-state p {
    margin-bottom: 1.5rem;
}

.pagination-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
}

.pagination {
    display: flex;
    gap: 0.5rem;
    align-items: center;
}

.pagination-btn {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.25rem;
    background: var(--color-white);
    border: 2px solid var(--color-gray-200);
    color: var(--color-gray-700);
    border-radius: var(--radius-lg);
    text-decoration: none;
    font-weight: 600;
    transition: all var(--transition-fast);
}

.pagination-btn:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
}

.pagination-numbers {
    display: flex;
    gap: 0.5rem;
}

.pagination-number {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-white);
    border: 2px solid var(--color-gray-200);
    color: var(--color-gray-700);
    border-radius: var(--radius-lg);
    text-decoration: none;
    font-weight: 600;
    transition: all var(--transition-fast);
}

.pagination-number:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
}

.pagination-number.active {
    background: var(--color-primary-gradient);
    border-color: transparent;
    color: var(--color-white);
}

.pagination-ellipsis {
    padding: 0 0.5rem;
    color: var(--color-gray-400);
    font-weight: 700;
}

.pagination-info {
    color: var(--color-gray-500);
    font-size: 0.95rem;
}

@media (max-width: 968px) {
    .filter-toolbar {
        flex-direction: column;
        align-items: stretch;
    }
    
    .search-form {
        max-width: 100%;
    }
    
    .filter-options {
        width: 100%;
        justify-content: stretch;
    }
    
    .filter-select-wrapper {
        flex: 1;
    }
    
    .filter-select {
        width: 100%;
    }
    
    .products-grid {
        grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    }
}

@media (max-width: 640px) {
    .page-title {
        font-size: 1.75rem;
    }
    
    .products-grid {
        grid-template-columns: 1fr;
    }
    
    .pagination {
        flex-wrap: wrap;
        justify-content: center;
    }
    
    .pagination-btn {
        order: -1;
        width: 100%;
        justify-content: center;
    }
}
</style>

<?php require __DIR__ . '/_footer.phtml'; ?>
