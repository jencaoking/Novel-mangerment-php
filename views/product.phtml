<?php require __DIR__ . '/_header.phtml'; ?>

<section class="product-detail-section">
    <input type="hidden" id="csrf_token" value="<?= generateCSRFToken() ?>">
    <div class="container">
        <nav class="breadcrumb" aria-label="breadcrumb">
            <a href="/" class="breadcrumb-item">首页</a>
            <span class="breadcrumb-separator">/</span>
            <a href="/<?= $product['type'] === 'music' ? 'music' : 'novels' ?>" class="breadcrumb-item">
                <?= $product['type'] === 'music' ? '音乐' : '小说' ?>
            </a>
            <span class="breadcrumb-separator">/</span>
            <span class="breadcrumb-item active" aria-current="page"><?= e($product['title']) ?></span>
        </nav>

        <?php if ($message): ?>
            <div class="alert alert-success alert-dismissible fade-in">
                <i class="bi bi-check-circle-fill"></i>
                <span><?= e($message) ?></span>
            </div>
        <?php endif; ?>

        <div class="product-detail-grid">
            <div class="product-image-gallery">
                <div class="product-cover-wrapper">
                    <img src="/uploads/cover/<?= e($product['cover'] ?: 'default.jpg') ?>"
                         alt="<?= e($product['title']) ?>"
                         class="product-cover-main">
                    <div class="product-type-badge badge-<?= $product['type'] ?>">
                        <i class="bi bi-<?= $product['type'] === 'music' ? 'music-note-beamed' : 'book' ?>"></i>
                        <?= $product['type'] === 'music' ? '音乐' : '小说' ?>
                    </div>
                </div>

                <?php if ($product['type'] === 'music' && $product['preview_path']): ?>
                    <div class="music-preview-section">
                        <h4 class="preview-title"><i class="bi bi-play-circle"></i> 试听片段</h4>
                        <div class="audio-player-container">
                            <audio controls class="custom-audio">
                                <source src="/uploads/preview/<?= e($product['preview_path']) ?>" type="audio/mpeg">
                                您的浏览器不支持音频播放。
                            </audio>
                        </div>
                    </div>
                <?php endif; ?>
            </div>

            <div class="product-info-panel">
                <div class="product-header">
                    <span class="product-category">
                        <i class="bi bi-tag"></i>
                        <?= e($product['category_name'] ?? '精选') ?>
                    </span>
                    <h1 class="product-title"><?= e($product['title']) ?></h1>
                    <p class="product-creator">
                        <i class="bi bi-person-circle"></i>
                        <?= $product['type'] === 'novel' ? '作者' : '艺术家' ?>：
                        <strong><?= e($product['author']) ?></strong>
                    </p>
                </div>

                <div class="product-metrics">
                    <div class="metric-item">
                        <span class="metric-label">价格</span>
                        <span class="metric-value price"><?= formatPrice($product['price']) ?></span>
                    </div>
                    <div class="metric-item">
                        <span class="metric-label">销量</span>
                        <span class="metric-value"><?= number_format($product['sales']) ?></span>
                    </div>
                    <div class="metric-item">
                        <span class="metric-label">下载</span>
                        <span class="metric-value"><?= number_format($product['downloads']) ?></span>
                    </div>
                    <div class="metric-item">
                        <span class="metric-label">评分</span>
                        <span class="metric-value">
                            <div class="stars-display">
                                <?php for($i = 1; $i <= 5; $i++): ?>
                                    <i class="bi <?= ($i <= (int)$product['rating_avg']) ? 'bi-star-fill' : 'bi-star' ?>"></i>
                                <?php endfor; ?>
                            </div>
                            <span class="rating-number"><?= number_format($product['rating_avg'] ?? 0, 1) ?></span>
                        </span>
                    </div>
                </div>

                <div class="product-description-section">
                    <h3 class="section-heading"><i class="bi bi-info-circle"></i> 商品介绍</h3>
                    <div class="description-content">
                        <p><?= nl2br(e($product['description'])) ?></p>
                    </div>
                </div>

                <div class="product-actions">
                    <?php if ($hasPurchased): ?>
                        <a href="/user/downloads" class="btn btn-primary btn-large btn-block">
                            <i class="bi bi-cloud-download"></i> 前往下载已购商品
                        </a>
                        <div class="purchase-status-badge">
                            <i class="bi bi-check-circle"></i> 已购买
                        </div>
                    <?php else: ?>
                        <button type="button" class="btn btn-secondary btn-large" onclick="addToCart(<?= $productId ?>, this)">
                            <i class="bi bi-cart-plus"></i> 加入购物车
                        </button>
                        <form method="POST" action="">
                            <input type="hidden" name="csrf_token" value="<?= generateCSRFToken() ?>">
                            <button type="submit" name="buy" class="btn btn-primary btn-large btn-gradient">
                                <i class="bi bi-bag-check"></i> 立即购买
                            </button>
                        </form>
                    <?php endif; ?>
                </div>
            </div>
        </div>

        <!-- 评价区域 -->
        <div class="reviews-section">
            <div class="reviews-header">
                <h2 class="section-title"><i class="bi bi-chat-dots"></i> 用户评价</h2>
                <span class="reviews-count">(<?= count($reviews) ?> 条)</span>
            </div>

            <?php if ($hasPurchased && !$hasReviewed): ?>
                <div class="review-form-card fade-in">
                    <h3 class="form-title"><i class="bi bi-pencil-square"></i> 分享您的体验</h3>
                    <form id="reviewForm" onsubmit="submitReview(event, <?= $productId ?>)">
                        <div class="form-group">
                            <label class="form-label">评分</label>
                            <div class="rating-selector" id="starRating">
                                <i class="bi bi-star rating-star" data-rating="1"></i>
                                <i class="bi bi-star rating-star" data-rating="2"></i>
                                <i class="bi bi-star rating-star" data-rating="3"></i>
                                <i class="bi bi-star rating-star" data-rating="4"></i>
                                <i class="bi bi-star rating-star" data-rating="5"></i>
                                <input type="hidden" name="rating" id="selectedRating" value="5">
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="form-label">评价内容</label>
                            <textarea name="content" id="reviewContent" rows="4" class="form-control" 
                                      placeholder="说说您的使用体验..." required></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-send"></i> 提交评价
                        </button>
                    </form>
                </div>
            <?php elseif ($hasPurchased && $hasReviewed): ?>
                <div class="alert alert-info">
                    <i class="bi bi-info-circle"></i> 您已评价过此商品，感谢您的反馈！
                </div>
            <?php endif; ?>

            <div class="reviews-list">
                <?php if (empty($reviews)): ?>
                    <div class="empty-state">
                        <i class="bi bi-emoji-smile"></i>
                        <p>暂无评价，快来抢沙发吧！</p>
                    </div>
                <?php else: ?>
                    <?php foreach ($reviews as $review): ?>
                        <div class="review-item fade-in">
                            <div class="reviewer-info">
                                <div class="reviewer-avatar">
                                    <?= strtoupper(substr($review['username'], 0, 1)) ?>
                                </div>
                                <div class="reviewer-details">
                                    <h4 class="reviewer-name"><?= e($review['username']) ?></h4>
                                    <div class="review-meta">
                                        <div class="stars-mini">
                                            <?php for($i = 1; $i <= 5; $i++): ?>
                                                <i class="bi <?= ($i <= $review['rating']) ? 'bi-star-fill' : 'bi-star' ?>"></i>
                                            <?php endfor; ?>
                                        </div>
                                        <span class="review-date">
                                            <i class="bi bi-clock"></i> 
                                            <?= date('Y-m-d H:i', strtotime($review['create_time'])) ?>
                                        </span>
                                        <span class="verified-purchase">
                                            <i class="bi bi-check-circle-fill"></i> 已购买
                                        </span>
                                    </div>
                                </div>
                            </div>
                            <div class="review-content">
                                <p><?= nl2br(e($review['content'])) ?></p>
                            </div>
                        </div>
                    <?php endforeach; ?>
                <?php endif; ?>
            </div>
        </div>
    </div>
</section>

<script src="/assets/js/main.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function() {
    // 评分星星选择器
    const starRating = document.getElementById('starRating');
    const selectedRating = document.getElementById('selectedRating');
    
    if (starRating) {
        const stars = starRating.querySelectorAll('.rating-star');
        
        // 默认选中5星
        updateStars(5);
        
        stars.forEach(star => {
            star.addEventListener('click', function() {
                const rating = parseInt(this.dataset.rating);
                selectedRating.value = rating;
                updateStars(rating);
            });
            
            star.addEventListener('mouseenter', function() {
                const rating = parseInt(this.dataset.rating);
                previewStars(rating);
            });
        });
        
        starRating.addEventListener('mouseleave', function() {
            updateStars(parseInt(selectedRating.value));
        });
    }
    
    function updateStars(rating) {
        const stars = starRating.querySelectorAll('.rating-star');
        stars.forEach(star => {
            const starRating = parseInt(star.dataset.rating);
            if (starRating <= rating) {
                star.classList.add('bi-star-fill');
                star.classList.remove('bi-star');
                star.style.color = '#ffc107';
            } else {
                star.classList.remove('bi-star-fill');
                star.classList.add('bi-star');
                star.style.color = '#ddd';
            }
        });
    }
    
    function previewStars(rating) {
        const stars = starRating.querySelectorAll('.rating-star');
        stars.forEach(star => {
            const starRating = parseInt(star.dataset.rating);
            if (starRating <= rating) {
                star.classList.add('bi-star-fill');
                star.classList.remove('bi-star');
                star.style.color = '#ffc107';
            } else {
                star.classList.remove('bi-star-fill');
                star.classList.add('bi-star');
                star.style.color = '#ddd';
            }
        });
    }
});

async function addToCart(productId, btn) {
    const originalHTML = btn.innerHTML;
    
    btn.disabled = true;
    btn.innerHTML = '<span class="spinner-border spinner-border-sm"></span> 加入中...';
    
    try {
        const csrfToken = document.getElementById('csrf_token').value;
        const response = await fetch('/api/cart/add', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: `product_id=${productId}&csrf_token=${encodeURIComponent(csrfToken)}`
        });
        const data = await response.json();
        
        if (data.success) {
            showNotification('已加入购物车', 'success');
            updateCartCount(data.cart_count);
        } else {
            showNotification(data.message || '加入失败', 'error');
        }
    } catch (err) {
        console.error(err);
        showNotification('网络错误，请稍后再试', 'error');
    } finally {
        btn.disabled = false;
        btn.innerHTML = originalHTML;
    }
}

async function submitReview(event, productId) {
    event.preventDefault();
    const btn = event.target.querySelector('button[type="submit"]');
    const originalHTML = btn.innerHTML;
    
    btn.disabled = true;
    btn.innerHTML = '<span class="spinner-border spinner-border-sm"></span> 提交中...';
    
    const rating = document.getElementById('selectedRating').value;
    const content = document.getElementById('reviewContent').value;
    const csrfToken = document.getElementById('csrf_token').value;
    
    try {
        const response = await fetch(`/product/review/${productId}`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
            body: `rating=${rating}&content=${encodeURIComponent(content)}&csrf_token=${encodeURIComponent(csrfToken)}`
        });
        const data = await response.json();
        
        if (data.success) {
            showNotification('评价成功！', 'success');
            setTimeout(() => window.location.reload(), 1000);
        } else {
            showNotification(data.message || '提交失败', 'error');
        }
    } catch (err) {
        console.error(err);
        showNotification('网络错误，请稍后再试', 'error');
    } finally {
        btn.disabled = false;
        btn.innerHTML = originalHTML;
    }
}
</script>

<style>
.product-detail-section {
    padding: 100px 0 60px;
    background: linear-gradient(180deg, var(--color-gray-50) 0%, var(--color-white) 100%);
}

.breadcrumb {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 2rem;
    font-size: 0.9rem;
}

.breadcrumb-item {
    color: var(--color-gray-600);
}

.breadcrumb-item.active {
    color: var(--color-primary);
    font-weight: 500;
}

.breadcrumb-separator {
    color: var(--color-gray-400);
}

.product-detail-grid {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 3rem;
    margin-bottom: 4rem;
}

.product-image-gallery {
    position: sticky;
    top: 100px;
}

.product-cover-wrapper {
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
}

.product-cover-main {
    width: 100%;
    aspect-ratio: 3/4;
    object-fit: cover;
}

.product-type-badge {
    position: absolute;
    top: 1rem;
    left: 1rem;
    padding: 0.4rem 1rem;
    border-radius: var(--radius-full);
    font-weight: 600;
    font-size: 0.85rem;
    display: flex;
    align-items: center;
    gap: 0.3rem;
}

.badge-novel {
    background: rgba(102, 126, 234, 0.95);
    color: white;
}

.badge-music {
    background: rgba(118, 75, 162, 0.95);
    color: white;
}

.music-preview-section {
    margin-top: 1.5rem;
    padding: 1.5rem;
    background: var(--color-white);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
}

.preview-title {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: var(--color-gray-700);
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.custom-audio {
    width: 100%;
    border-radius: var(--radius-md);
}

.product-info-panel {
    background: var(--color-white);
    padding: 2rem;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
}

.product-header {
    margin-bottom: 2rem;
}

.product-category {
    display: inline-block;
    padding: 0.4rem 0.8rem;
    background: var(--color-gray-100);
    color: var(--color-primary);
    font-size: 0.8rem;
    font-weight: 600;
    border-radius: var(--radius-full);
    margin-bottom: 0.8rem;
    display: flex;
    align-items: center;
    gap: 0.4rem;
}

.product-title {
    font-size: 2rem;
    margin-bottom: 0.5rem;
    font-weight: 700;
    line-height: 1.3;
}

.product-creator {
    color: var(--color-gray-600);
    font-size: 1.05rem;
    display: flex;
    align-items: center;
    gap: 0.4rem;
}

.product-metrics {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    padding: 1.5rem;
    background: var(--color-gray-50);
    border-radius: var(--radius-lg);
    margin-bottom: 2rem;
}

.metric-item {
    text-align: center;
}

.metric-label {
    display: block;
    font-size: 0.8rem;
    color: var(--color-gray-500);
    margin-bottom: 0.3rem;
}

.metric-value {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--color-gray-800);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.3rem;
}

.metric-value.price {
    color: var(--color-primary);
    font-size: 1.8rem;
}

.stars-display {
    display: inline-flex;
    gap: 0.15rem;
}

.stars-display i,
.stars-mini i {
    color: #ffc107;
    font-size: 0.9rem;
}

.rating-number {
    margin-left: 0.3rem;
}

.product-description-section {
    margin-bottom: 2rem;
}

.section-heading {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--color-gray-800);
    margin-bottom: 0.8rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.description-content {
    color: var(--color-gray-600);
    line-height: 1.7;
}

.product-actions {
    display: flex;
    gap: 1rem;
    flex-direction: column;
}

.btn-large {
    padding: 1rem 1.5rem;
    font-size: 1.05rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    font-weight: 600;
}

.btn-gradient {
    background: var(--color-primary-gradient);
    border: none;
}

.btn-block {
    width: 100%;
}

.purchase-status-badge {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.75rem;
    background: #e8f5e9;
    color: #2e7d32;
    border-radius: var(--radius-md);
    font-weight: 600;
}

.reviews-section {
    padding-top: 2rem;
}

.reviews-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 2rem;
}

.section-title {
    font-size: 1.5rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.reviews-count {
    font-size: 1rem;
    color: var(--color-gray-500);
}

.review-form-card {
    background: var(--color-white);
    padding: 2rem;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    margin-bottom: 2rem;
}

.form-title {
    font-size: 1.1rem;
    margin-bottom: 1.5rem;
    font-weight: 600;
}

.form-label {
    display: block;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--color-gray-700);
}

.rating-selector {
    display: inline-flex;
    gap: 0.5rem;
    margin-bottom: 1rem;
}

.rating-selector .rating-star {
    font-size: 1.8rem;
    color: #ddd;
    cursor: pointer;
    transition: all 0.2s ease;
}

.rating-selector .rating-star:hover {
    transform: scale(1.1);
}

.reviews-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.review-item {
    background: var(--color-white);
    padding: 1.5rem;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
}

.reviewer-info {
    display: flex;
    gap: 1rem;
    margin-bottom: 1rem;
}

.reviewer-avatar {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--color-primary-gradient);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    font-size: 1.2rem;
}

.reviewer-details {
    flex: 1;
}

.reviewer-name {
    font-weight: 600;
    margin-bottom: 0.3rem;
    color: var(--color-gray-800);
}

.review-meta {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    font-size: 0.85rem;
    color: var(--color-gray-500);
}

.verified-purchase {
    color: #2e7d32;
    display: flex;
    align-items: center;
    gap: 0.3rem;
    font-weight: 500;
}

.review-content {
    color: var(--color-gray-600);
    line-height: 1.7;
}

.empty-state {
    text-align: center;
    padding: 3rem;
    color: var(--color-gray-500);
}

.empty-state i {
    font-size: 4rem;
    margin-bottom: 1rem;
    opacity: 0.3;
}

.fade-in {
    animation: fadeIn 0.5s ease-out;
}

@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

@media (max-width: 968px) {
    .product-detail-grid {
        grid-template-columns: 1fr;
    }
    
    .product-image-gallery {
        position: static;
    }
    
    .product-metrics {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width: 640px) {
    .product-title {
        font-size: 1.5rem;
    }
    
    .product-metrics {
        grid-template-columns: 1fr 1fr;
    }
}
</style>

<?php require __DIR__ . '/_footer.phtml'; ?>
