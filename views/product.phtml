<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= e($product['title']) ?> - BookMusic Mall</title>
    <link rel="stylesheet" href="/assets/css/style.css">
    <link rel="icon" type="image/svg+xml" href="/assets/images/favicon.svg">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <div class="navbar-content">
                <a href="/" class="navbar-brand">
                    <svg viewBox="0 0 40 40" xmlns="http://www.w3.org/2000/svg">
                        <path d="M20 2C10.06 2 2 10.06 2 20s8.06 18 18 18 18-8.06 18-18S29.94 2 20 2zm0 4c7.73 0 14 6.27 14 14s-6.27 14-14 14S6 25.73 6 20 12.27 6 20 6zm-4 6v16l12-8-12-8z"/>
                    </svg>
                    <span>BookMusic Mall</span>
                </a>
                
                <ul class="navbar-nav">
                    <li><a href="/">首页</a></li>
                    <li><a href="/novels">小说</a></li>
                    <li><a href="/music">音乐</a></li>
                    <?php if (isLoggedIn()): ?>
                        <li><a href="/cart" class="cart-link">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" style="vertical-align: middle;">
                                <path d="M7 18c-1.1 0-1.99.9-1.99 2S5.9 22 7 22s2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96 0 1.1.9 2 2 2h12v-2H7.42c-.14 0-.25-.11-.25-.25l.03-.12.9-1.63h7.45c.75 0 1.41-.41 1.75-1.03l3.58-6.49c.08-.14.12-.31.12-.48 0-.55-.45-1-1-1H5.21l-.94-2H1zm16 16c-1.1 0-1.99.9-1.99 2s.89 2 1.99 2 2-.9 2-2-.9-2-2-2z"/>
                            </svg>
                            购物车
                        </a></li>
                    <?php endif; ?>
                </ul>
                
                <div class="navbar-actions">
                    <?php if (isLoggedIn()): ?>
                        <a href="/user/" class="btn btn-secondary">用户中心</a>
                        <a href="/logout" class="btn btn-outline">退出</a>
                    <?php else: ?>
                        <a href="/login" class="btn btn-secondary">登录</a>
                        <a href="/register" class="btn btn-primary">注册</a>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </nav>

    <section class="product-detail">
        <div class="container">
            <?php if ($message): ?>
                <div class="alert alert-success">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
                    </svg>
                    <?= e($message) ?>
                </div>
            <?php endif; ?>
            
            <div class="product-detail-grid">
                <div class="product-image-section">
                    <img src="/uploads/cover/<?= e($product['cover']) ?>" 
                         alt="<?= e($product['title']) ?>" 
                         class="product-cover">
                    
                    <?php if ($product['type'] === 'music' && $product['preview_path']): ?>
                        <div class="music-player">
                            <h4>试听</h4>
                            <audio controls>
                                <source src="/uploads/preview/<?= e($product['preview_path']) ?>" type="audio/mpeg">
                                您的浏览器不支持音频播放。
                            </audio>
                        </div>
                    <?php endif; ?>
                </div>
                
                <div class="product-info-section">
                    <span class="product-category-badge">
                        <?= e($product['category_name'] ?? ($product['type'] === 'novel' ? '小说' : '音乐')) ?>
                    </span>
                    
                    <h1 class="product-title"><?= e($product['title']) ?></h1>
                    
                    <p class="product-author">
                        <?= $product['type'] === 'novel' ? '作者' : '歌手' ?>：
                        <strong><?= e($product['author']) ?></strong>
                    </p>
                    
                    <div class="product-stats">
            <div class="stat-item">
                <span class="stat-label">价格</span>
                <span class="stat-value price"><?= formatPrice($product['price']) ?></span>
            </div>
            <div class="stat-item">
                <span class="stat-label">销量</span>
                <span class="stat-value"><?= $product['sales'] ?></span>
            </div>
            <div class="stat-item">
                <span class="stat-label">下载量</span>
                <span class="stat-value"><?= $product['downloads'] ?></span>
            </div>
            <div class="stat-item">
                <span class="stat-label">评分</span>
                <span class="stat-value">
                    <div class="star-rating" data-rating="<?= $product['rating_avg'] ?? 0 ?>">
                        <?php for($i = 1; $i <= 5; $i++): ?>
                            <span class="star <?= ($i <= ($product['rating_avg'] ?? 0)) ? 'filled' : '' ?>">★</span>
                        <?php endfor; ?>
                    </div>
                    <span class="rating-text"><?= number_format($product['rating_avg'] ?? 0, 1) ?></span>
                </span>
            </div>
            <div class="stat-item">
                <span class="stat-label">评价</span>
                <span class="stat-value"><?= $product['review_count'] ?? 0 ?></span>
            </div>
        </div>
                    
                    <div class="product-description">
                        <h3>简介</h3>
                        <p><?= nl2br(e($product['description'])) ?></p>
                    </div>
                    
                    <div class="product-actions">
                        <?php if ($hasPurchased): ?>
                            <a href="/user/downloads" class="btn btn-primary btn-lg">
                                <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                    <path d="M19 9h-4V3H9v6H5l7 7 7-7zM5 18v2h14v-2H5z"/>
                                </svg>
                                去下载
                            </a>
                        <?php else: ?>
                            <button type="button" class="btn btn-secondary btn-lg" onclick="addToCart(<?= $productId ?>)">
                                <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                    <path d="M11 9h2V6h-2v3zm-2 7c0 1.1.9 2 2 2s2-.9 2-2-.9-2-2-2-2 .9-2 2zm9-13h-6v2h6v13H6V8H4v9c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2z"/>
                                </svg>
                                加入购物车
                            </button>
                            <form method="POST" action="">
                                <input type="hidden" name="csrf_token" value="<?= generateCSRFToken() ?>">
                                <button type="submit" name="buy" class="btn btn-primary btn-lg">
                                    <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                        <path d="M7 18c-1.1 0-1.99.9-1.99 2S5.9 22 7 22s2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96 0 1.1.9 2 2 2h12v-2H7.42c-.14 0-.25-.11-.25-.25l.03-.12.9-1.63h7.45c.75 0 1.41-.41 1.75-1.03l3.58-6.49c.08-.14.12-.31.12-.48 0-.55-.45-1-1-1H5.21l-.94-2H1zm16 16c-1.1 0-1.99.9-1.99 2s.89 2 1.99 2 2-.9 2-2-.9-2-2-2z"/>
                                    </svg>
                                    立即购买
                                </button>
                            </form>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- 评价区域 -->
    <section class="reviews-section">
        <div class="container">
            <h2 class="section-title">商品评价</h2>
            
            <?php if ($reviewCount > 0): ?>
                <div class="reviews-summary">
                    <div class="rating-overview">
                        <div class="big-rating">
                            <span class="rating-number"><?= number_format($product['rating_avg'] ?? 0, 1) ?></span>
                            <div class="stars">
                                <?php for($i = 1; $i <= 5; $i++): ?>
                                    <span class="star <?= ($i <= ($product['rating_avg'] ?? 0)) ? 'filled' : '' ?>">★</span>
                                <?php endfor; ?>
                            </div>
                            <span class="review-count">(<?= $reviewCount ?> 条评价)</span>
                        </div>
                        <div class="rating-bars">
                            <?php 
                            $totalReviews = $reviewCount;
                            for($star = 5; $star >= 1; $star--):
                                $count = 0;
                                foreach($ratingDistribution as $dist) {
                                    if ($dist['rating'] == $star) {
                                        $count = $dist['count'];
                                        break;
                                    }
                                }
                                $percentage = $totalReviews > 0 ? ($count / $totalReviews) * 100 : 0;
                            ?>
                                <div class="rating-bar-row">
                                    <span class="star-label"><?= $star ?> 星</span>
                                    <div class="bar-container">
                                        <div class="bar-fill" style="width: <?= $percentage ?>%"></div>
                                    </div>
                                    <span class="bar-count"><?= $count ?></span>
                                </div>
                            <?php endfor; ?>
                        </div>
                    </div>
                </div>
            <?php endif; ?>

            <!-- 评价表单 -->
            <?php if (isLoggedIn() && $hasPurchased && !$userReview): ?>
                <div class="review-form-container">
                    <h3>发表评价</h3>
                    <?php if ($message): ?>
                        <div class="alert alert-info"><?= e($message) ?></div>
                    <?php endif; ?>
                    <form method="POST" action="/product/<?= $productId ?>/review" class="review-form">
                        <input type="hidden" name="csrf_token" value="<?= generateCSRFToken() ?>">
                        <div class="form-group">
                            <label>评分</label>
                            <div class="rating-selector">
                                <?php for($i = 5; $i >= 1; $i--): ?>
                                    <input type="radio" id="star<?= $i ?>" name="rating" value="<?= $i ?>" <?= $i == 5 ? 'checked' : '' ?>>
                                    <label for="star<?= $i ?>" class="rating-star">★</label>
                                <?php endfor; ?>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="content">评价内容</label>
                            <textarea id="content" name="content" rows="4" placeholder="分享您的使用体验..."></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary">提交评价</button>
                    </form>
                </div>
            <?php elseif (isLoggedIn() && $hasPurchased && $userReview): ?>
                <div class="alert alert-success">
                    您已评价过此商品，感谢您的评价！
                </div>
            <?php elseif (isLoggedIn() && !$hasPurchased): ?>
                <div class="alert alert-info">
                    购买此商品后即可发表评价
                </div>
            <?php else: ?>
                <div class="alert alert-info">
                    <a href="/login">登录</a> 并购买商品后即可发表评价
                </div>
            <?php endif; ?>

            <!-- 评价列表 -->
            <?php if (count($reviews) > 0): ?>
                <div class="reviews-list">
                    <?php foreach($reviews as $review): ?>
                        <div class="review-item">
                            <div class="review-header">
                                <div class="reviewer-info">
                                    <div class="reviewer-avatar">
                                        <?= $review['avatar'] ? '<img src="/uploads/avatar/' . e($review['avatar']) . '" alt="">' : mb_substr($review['username'], 0, 1) ?>
                                    </div>
                                    <div class="reviewer-details">
                                        <span class="reviewer-name"><?= e($review['username']) ?></span>
                                        <div class="review-stars">
                                            <?php for($i = 1; $i <= 5; $i++): ?>
                                                <span class="star <?= ($i <= $review['rating']) ? 'filled' : '' ?>">★</span>
                                            <?php endfor; ?>
                                        </div>
                                    </div>
                                </div>
                                <span class="review-date"><?= date('Y-m-d H:i', strtotime($review['create_time'])) ?></span>
                            </div>
                            <?php if ($review['content']): ?>
                                <div class="review-content">
                                    <p><?= nl2br(e($review['content'])) ?></p>
                                </div>
                            <?php endif; ?>
                            <div class="verified-badge">
                                <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">
                                    <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
                                </svg>
                                已购买
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            <?php else: ?>
                <div class="no-reviews">
                    暂无评价，快来发表第一条评价吧！
                </div>
            <?php endif; ?>
        </div>
    </section>

    <footer class="footer">
        <div class="container">
            <div class="footer-bottom">
                <p>&copy; <?= date('Y') ?> BookMusic Mall. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="/assets/js/main.js"></script>
    <script>
        // 加入购物车
        function addToCart(productId) {
            fetch('/api/cart/add', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded',
                },
                body: `product_id=${productId}`
            })
            .then(res => res.json())
            .then(data => {
                if (data.success) {
                    alert('已加入购物车');
                } else {
                    alert(data.message || '加入失败');
                }
            })
            .catch(err => {
                console.error('Error:', err);
                alert('网络错误，请稍后再试');
            });
        }
    </script>
    
    <style>
        .product-detail {
            padding-top: 120px;
            min-height: calc(100vh - 80px);
        }
        
        .alert {
            display: flex;
            align-items: center;
            gap: var(--spacing-sm);
            padding: var(--spacing-md);
            border-radius: var(--radius-md);
            margin-bottom: var(--spacing-xl);
        }
        
        .alert-success {
            background: #efe;
            color: #2e7d32;
            border: 1px solid #cfc;
        }
        
        .alert-info {
            background: #e3f2fd;
            color: #1565c0;
            border: 1px solid #bbdefb;
        }
        
        .product-detail-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: var(--spacing-3xl);
            margin-bottom: var(--spacing-3xl);
        }
        
        .product-image-section {
            position: sticky;
            top: 100px;
        }
        
        .product-cover {
            width: 100%;
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-xl);
        }
        
        .music-player {
            margin-top: var(--spacing-xl);
            padding: var(--spacing-lg);
            background: var(--white);
            border-radius: var(--radius-md);
            box-shadow: var(--shadow-md);
        }
        
        .music-player h4 {
            margin-bottom: var(--spacing-md);
        }
        
        .music-player audio {
            width: 100%;
        }
        
        .product-info-section {
            padding: var(--spacing-xl);
            background: var(--white);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-md);
        }
        
        .product-category-badge {
            display: inline-block;
            padding: var(--spacing-xs) var(--spacing-md);
            background: var(--accent-light);
            color: var(--accent-dark);
            font-size: 0.85rem;
            font-weight: 600;
            border-radius: var(--radius-sm);
            margin-bottom: var(--spacing-md);
        }
        
        .product-title {
            font-size: 2.5rem;
            margin-bottom: var(--spacing-md);
        }
        
        .product-author {
            font-size: 1.1rem;
            color: var(--gray-600);
            margin-bottom: var(--spacing-xl);
        }
        
        .product-stats {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: var(--spacing-lg);
            padding: var(--spacing-lg);
            background: var(--gray-50);
            border-radius: var(--radius-md);
            margin-bottom: var(--spacing-xl);
        }
        
        .stat-item {
            text-align: center;
        }
        
        .stat-label {
            display: block;
            font-size: 0.85rem;
            color: var(--gray-600);
            margin-bottom: var(--spacing-xs);
        }
        
        .stat-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-color);
        }
        
        .stat-value.price {
            color: var(--accent-color);
        }
        
        .star-rating {
            display: inline-flex;
            gap: 2px;
            margin-bottom: 4px;
        }
        
        .star-rating .star,
        .stars .star,
        .review-stars .star {
            color: #ddd;
            font-size: 1rem;
        }
        
        .star-rating .star.filled,
        .stars .star.filled,
        .review-stars .star.filled {
            color: #ffc107;
        }
        
        .rating-text {
            font-size: 1rem;
            color: var(--gray-700);
            margin-left: 4px;
        }
        
        .product-description {
            margin-bottom: var(--spacing-xl);
        }
        
        .product-description h3 {
            margin-bottom: var(--spacing-md);
        }
        
        .product-description p {
            line-height: 1.8;
            color: var(--gray-700);
        }
        
        .product-actions {
            display: flex;
            gap: var(--spacing-md);
        }
        
        .btn-lg {
            padding: 1rem 2rem;
            font-size: 1.1rem;
        }
        
        /* Reviews Section Styles */
        .reviews-section {
            padding: var(--spacing-3xl) 0;
            background: var(--gray-50);
        }
        
        .section-title {
            font-size: 1.8rem;
            margin-bottom: var(--spacing-xl);
            color: var(--gray-900);
        }
        
        .reviews-summary {
            background: var(--white);
            padding: var(--spacing-xl);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            margin-bottom: var(--spacing-xl);
        }
        
        .rating-overview {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: var(--spacing-xl);
            align-items: center;
        }
        
        .big-rating {
            text-align: center;
        }
        
        .rating-number {
            font-size: 3rem;
            font-weight: 700;
            color: var(--primary-color);
            display: block;
        }
        
        .big-rating .stars {
            margin: var(--spacing-sm) 0;
        }
        
        .big-rating .stars .star {
            font-size: 1.5rem;
        }
        
        .review-count {
            color: var(--gray-600);
        }
        
        .rating-bars {
            display: flex;
            flex-direction: column;
            gap: var(--spacing-sm);
        }
        
        .rating-bar-row {
            display: flex;
            align-items: center;
            gap: var(--spacing-md);
        }
        
        .star-label {
            width: 50px;
            font-size: 0.9rem;
            color: var(--gray-700);
        }
        
        .bar-container {
            flex: 1;
            height: 8px;
            background: #eee;
            border-radius: 4px;
            overflow: hidden;
        }
        
        .bar-fill {
            height: 100%;
            background: linear-gradient(90deg, #ffc107, #ff9800);
            border-radius: 4px;
            transition: width 0.3s ease;
        }
        
        .bar-count {
            width: 40px;
            text-align: right;
            color: var(--gray-600);
            font-size: 0.9rem;
        }
        
        .review-form-container {
            background: var(--white);
            padding: var(--spacing-xl);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            margin-bottom: var(--spacing-xl);
        }
        
        .review-form-container h3 {
            margin-bottom: var(--spacing-lg);
            color: var(--gray-900);
        }
        
        .review-form {
            max-width: 600px;
        }
        
        .form-group {
            margin-bottom: var(--spacing-lg);
        }
        
        .form-group label {
            display: block;
            margin-bottom: var(--spacing-sm);
            font-weight: 600;
            color: var(--gray-800);
        }
        
        .rating-selector {
            display: flex;
            flex-direction: row-reverse;
            justify-content: flex-end;
            gap: 8px;
        }
        
        .rating-selector input[type="radio"] {
            display: none;
        }
        
        .rating-selector .rating-star {
            font-size: 2rem;
            color: #ddd;
            cursor: pointer;
            transition: color 0.2s;
        }
        
        .rating-selector input[type="radio"]:checked ~ .rating-star,
        .rating-selector .rating-star:hover,
        .rating-selector .rating-star:hover ~ .rating-star {
            color: #ffc107;
        }
        
        .form-group textarea {
            width: 100%;
            padding: var(--spacing-md);
            border: 1px solid var(--gray-300);
            border-radius: var(--radius-md);
            font-size: 1rem;
            resize: vertical;
            font-family: inherit;
        }
        
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(33, 150, 243, 0.1);
        }
        
        .reviews-list {
            display: flex;
            flex-direction: column;
            gap: var(--spacing-lg);
        }
        
        .review-item {
            background: var(--white);
            padding: var(--spacing-xl);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            position: relative;
        }
        
        .review-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: var(--spacing-md);
        }
        
        .reviewer-info {
            display: flex;
            align-items: center;
            gap: var(--spacing-md);
        }
        
        .reviewer-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea, #764ba2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
            font-size: 1.2rem;
            overflow: hidden;
        }
        
        .reviewer-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        .reviewer-details {
            display: flex;
            flex-direction: column;
            gap: 4px;
        }
        
        .reviewer-name {
            font-weight: 600;
            color: var(--gray-900);
        }
        
        .review-stars .star {
            font-size: 0.9rem;
        }
        
        .review-date {
            color: var(--gray-500);
            font-size: 0.9rem;
        }
        
        .review-content {
            margin-bottom: var(--spacing-md);
        }
        
        .review-content p {
            line-height: 1.7;
            color: var(--gray-700);
            margin: 0;
        }
        
        .verified-badge {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            padding: 4px 12px;
            background: #e8f5e9;
            color: #2e7d32;
            font-size: 0.8rem;
            border-radius: 12px;
            font-weight: 500;
        }
        
        .no-reviews {
            text-align: center;
            padding: var(--spacing-3xl);
            background: var(--white);
            border-radius: var(--radius-lg);
            color: var(--gray-500);
            font-size: 1.1rem;
        }
        
        @media (max-width: 768px) {
            .product-detail-grid {
                grid-template-columns: 1fr;
            }
            
            .product-image-section {
                position: static;
            }
            
            .product-stats {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .rating-overview {
                grid-template-columns: 1fr;
            }
        }
    </style>
</body>
</html>
