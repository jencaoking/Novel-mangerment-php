<?php $pageTitle = '我的下载'; ?>
<?php require __DIR__ . '/../_header.phtml'; ?>

<section class="user-center-section">
    <div class="container">
        <div class="user-layout">
            <div class="user-sidebar">
                <div class="user-info">
                    <div class="user-avatar-circle">
                        <i class="bi bi-person text-white" style="font-size: 2rem;"></i>
                    </div>
                    <h3><?= e($user['username']) ?></h3>
                    <p><?= e($user['email']) ?></p>
                </div>
                <nav class="user-nav">
                    <a href="/user/orders">
                        <i class="bi bi-receipt me-2"></i> 我的订单
                    </a>
                    <a href="/user/favorites">
                        <i class="bi bi-heart me-2"></i> 我的收藏
                    </a>
                    <a href="/user/downloads" class="active">
                        <i class="bi bi-download me-2"></i> 我的下载
                    </a>
                </nav>
            </div>
            
            <div class="user-content">
                <h1 class="page-title">我的下载</h1>
                
                <?php if (empty($purchasedProducts)): ?>
                    <div class="empty-state">
                        <i class="bi bi-inbox" style="font-size: 4rem; color: var(--color-gray-300);"></i>
                        <p>暂无已购买的商品</p>
                    </div>
                <?php else: ?>
                    <div class="downloads-list">
                        <?php foreach ($purchasedProducts as $product): ?>
                            <div class="download-card">
                                <img src="/uploads/cover/<?= e($product['cover'] ?: 'default.jpg') ?>" alt="<?= e($product['title']) ?>">
                                <div class="download-details">
                                    <h3><?= e($product['title']) ?></h3>
                                    <p class="download-info">
                                        <i class="bi <?= $product['type'] === 'novel' ? 'bi-book' : 'bi-music-note-beamed' ?> me-1"></i>
                                        <?= $product['type'] === 'novel' ? '作者' : '歌手' ?>：<?= e($product['author']) ?>
                                    </p>
                                    <p class="download-info">
                                        <i class="bi bi-clock me-1"></i>
                                        购买时间：<?= timeAgo($product['pay_time']) ?>
                                    </p>
                                </div>
                                <div class="download-actions">
                                    <a href="/download/<?= $product['product_id'] ?>" class="btn btn-primary">
                                        <i class="bi bi-download me-2"></i> 下载
                                    </a>
                                </div>
                            </div>
                        <?php endforeach; ?>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</section>

<style>
.user-center-section {
    padding: 80px 0;
    background: var(--color-gray-50);
    min-height: calc(100vh - 80px);
}

.user-layout {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 2rem;
}

.user-sidebar {
    position: sticky;
    top: 100px;
    height: fit-content;
}

.user-info {
    text-align: center;
    padding: 2rem;
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    margin-bottom: 1.5rem;
}

.user-avatar-circle {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    margin: 0 auto 1rem;
    background: var(--color-primary-gradient);
    display: flex;
    align-items: center;
    justify-content: center;
}

.user-info h3 {
    margin-bottom: 0.5rem;
    color: var(--color-gray-900);
}

.user-info p {
    margin: 0;
    color: var(--color-gray-500);
}

.user-nav {
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    padding: 0.5rem;
}

.user-nav a {
    display: flex;
    align-items: center;
    padding: 1rem 1.5rem;
    color: var(--color-gray-600);
    border-radius: var(--radius-lg);
    transition: all var(--transition-fast);
    text-decoration: none;
}

.user-nav a:hover, .user-nav a.active {
    background: var(--color-gray-50);
    color: var(--color-primary);
}

.user-nav a.active {
    background: var(--color-gray-50);
    color: var(--color-primary);
    font-weight: 600;
}

.user-content {
    background: var(--color-white);
    padding: 2.5rem;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
}

.page-title {
    margin-bottom: 2rem;
    color: var(--color-gray-900);
    font-weight: 700;
}

.empty-state {
    text-align: center;
    padding: 4rem 2rem;
    color: var(--color-gray-500);
}

.downloads-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.download-card {
    display: flex;
    gap: 1.5rem;
    padding: 1.5rem;
    background: var(--color-gray-50);
    border-radius: var(--radius-lg);
    align-items: center;
}

.download-card img {
    width: 120px;
    height: 120px;
    object-fit: cover;
    border-radius: var(--radius-md);
}

.download-details {
    flex: 1;
}

.download-details h3 {
    margin-bottom: 0.75rem;
    color: var(--color-gray-900);
    font-weight: 600;
}

.download-info {
    margin: 0.5rem 0;
    color: var(--color-gray-600);
    display: flex;
    align-items: center;
}

.download-actions {
    display: flex;
    align-items: center;
}

@media (max-width: 768px) {
    .user-layout {
        grid-template-columns: 1fr;
    }
    
    .download-card {
        flex-direction: column;
        text-align: center;
    }
    
    .download-card img {
        width: 100%;
        max-width: 200px;
        height: 200px;
    }
    
    .download-info {
        justify-content: center;
    }
}
</style>

<?php require __DIR__ . '/../_footer.phtml'; ?>
