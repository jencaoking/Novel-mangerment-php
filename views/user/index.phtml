<?php $pageTitle = '用户中心'; ?>
<?php require __DIR__ . '/../_header.phtml'; ?>

<section class="user-center-section">
    <div class="container">
        <div class="user-layout">
            <div class="user-sidebar">
                <div class="user-info">
                    <div class="user-avatar-circle">
                        <i class="bi bi-person text-white" style="font-size: 2rem;"></i>
                    </div>
                    <h3><?= e($user['username']) ?></h3>
                    <p><?= e($user['email']) ?></p>
                </div>
                <nav class="user-nav">
                    <a href="/user" class="active">
                        <i class="bi bi-speedometer2 me-2"></i> 概览
                    </a>
                    <a href="/user">
                        <i class="bi bi-person me-2"></i> 个人资料
                    </a>
                    <a href="/user/orders">
                        <i class="bi bi-receipt me-2"></i> 我的订单
                    </a>
                    <a href="/user/downloads">
                        <i class="bi bi-download me-2"></i> 我的下载
                    </a>
                </nav>
            </div>
            
            <div class="user-content">
                <h1 class="page-title">用户中心</h1>
                
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-receipt" style="font-size: 2.5rem; color: var(--color-primary);"></i>
                        </div>
                        <div class="stat-info">
                            <span class="stat-number"><?= $totalOrders ?></span>
                            <span class="stat-label">我的订单</span>
                        </div>
                    </div>
                    
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-credit-card" style="font-size: 2.5rem; color: var(--color-success);"></i>
                        </div>
                        <div class="stat-info">
                            <span class="stat-number"><?= formatPrice($totalSpent) ?></span>
                            <span class="stat-label">消费总额</span>
                        </div>
                    </div>
                </div>
                
                <div class="recent-section">
                    <h2 class="section-title">最近订单</h2>
                    
                    <?php if (empty($recentOrders)): ?>
                        <div class="empty-state">
                            <i class="bi bi-inbox" style="font-size: 3rem; color: var(--color-gray-300);"></i>
                            <p>暂无订单记录</p>
                        </div>
                    <?php else: ?>
                        <div class="orders-list">
                            <?php foreach ($recentOrders as $order): ?>
                                <div class="order-item">
                                    <img src="/uploads/cover/<?= e($order['cover_image'] ?: 'default.jpg') ?>" alt="<?= e($order['title']) ?>">
                                    <div class="order-info">
                                        <h4><?= e($order['title']) ?></h4>
                                        <p>订单号：<?= e($order['order_no']) ?></p>
                                        <p>金额：<?= formatPrice($order['price']) ?></p>
                                    </div>
                                    <div class="order-status">
                                        <span class="status-badge status-<?= getOrderStatusBadge($order['status']) ?>">
                                            <?= getOrderStatusText($order['status']) ?>
                                        </span>
                                        <span class="order-time">
                                            <i class="bi bi-clock me-1"></i>
                                            <?= timeAgo($order['create_time']) ?>
                                        </span>
                                    </div>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
.user-center-section {
    padding: 80px 0;
    background: var(--color-gray-50);
    min-height: calc(100vh - 80px);
}

.user-layout {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 2rem;
}

.user-sidebar {
    position: sticky;
    top: 100px;
    height: fit-content;
}

.user-info {
    text-align: center;
    padding: 2rem;
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    margin-bottom: 1.5rem;
}

.user-avatar-circle {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    margin: 0 auto 1rem;
    background: var(--color-primary-gradient);
    display: flex;
    align-items: center;
    justify-content: center;
}

.user-info h3 {
    margin-bottom: 0.5rem;
    color: var(--color-gray-900);
}

.user-info p {
    margin: 0;
    color: var(--color-gray-500);
}

.user-nav {
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    padding: 0.5rem;
}

.user-nav a {
    display: flex;
    align-items: center;
    padding: 1rem 1.5rem;
    color: var(--color-gray-600);
    border-radius: var(--radius-lg);
    transition: all var(--transition-fast);
    text-decoration: none;
}

.user-nav a:hover, .user-nav a.active {
    background: var(--color-gray-50);
    color: var(--color-primary);
}

.user-nav a.active {
    background: var(--color-gray-50);
    color: var(--color-primary);
    font-weight: 600;
}

.user-content {
    background: var(--color-white);
    padding: 2.5rem;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
}

.page-title {
    margin-bottom: 2rem;
    color: var(--color-gray-900);
    font-weight: 700;
}

.stats-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    margin-bottom: 2.5rem;
}

.stat-card {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    padding: 2rem;
    background: var(--color-gray-50);
    border-radius: var(--radius-lg);
}

.stat-icon {
    flex-shrink: 0;
}

.stat-number {
    display: block;
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--color-primary);
}

.stat-label {
    color: var(--color-gray-600);
    font-size: 1rem;
}

.recent-section {
    margin-top: 2rem;
}

.section-title {
    margin-bottom: 1.5rem;
    color: var(--color-gray-900);
    font-weight: 600;
}

.empty-state {
    text-align: center;
    padding: 3rem 2rem;
    color: var(--color-gray-500);
}

.orders-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.order-item {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    padding: 1.5rem;
    background: var(--color-gray-50);
    border-radius: var(--radius-md);
}

.order-item img {
    width: 80px;
    height: 80px;
    object-fit: cover;
    border-radius: var(--radius-sm);
}

.order-info {
    flex: 1;
}

.order-info h4 {
    margin-bottom: 0.5rem;
    color: var(--color-gray-900);
}

.order-info p {
    margin: 0.25rem 0;
    font-size: 0.95rem;
    color: var(--color-gray-600);
}

.order-status {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.5rem;
}

.status-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    border-radius: var(--radius-sm);
    font-size: 0.9rem;
    font-weight: 600;
}

.status-success {
    background: #d4edda;
    color: #155724;
}

.status-warning {
    background: #fff3cd;
    color: #856404;
}

.status-secondary {
    background: #f8f9fa;
    color: #6c757d;
}

.status-danger {
    background: #f8d7da;
    color: #721c24;
}

.order-time {
    display: block;
    font-size: 0.85rem;
    color: var(--color-gray-500);
}

@media (max-width: 768px) {
    .user-layout {
        grid-template-columns: 1fr;
    }
    
    .stats-grid {
        grid-template-columns: 1fr;
    }
    
    .order-item {
        flex-direction: column;
        text-align: center;
    }
    
    .order-item img {
        width: 100px;
        height: 100px;
    }
    
    .order-status {
        align-items: center;
    }
}
</style>

<?php require __DIR__ . '/../_footer.phtml'; ?>
