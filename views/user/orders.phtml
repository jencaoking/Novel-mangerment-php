<?php $pageTitle = '我的订单'; ?>
<?php require __DIR__ . '/../_header.phtml'; ?>

<section class="user-center-section">
    <div class="container">
        <div class="user-layout">
            <div class="user-sidebar">
                <div class="user-info">
                    <div class="user-avatar-circle">
                        <i class="bi bi-person text-white" style="font-size: 2rem;"></i>
                    </div>
                    <h3><?= e($user['username']) ?></h3>
                    <p><?= e($user['email']) ?></p>
                </div>
                <nav class="user-nav">
                    <a href="/user/orders" class="active">
                        <i class="bi bi-receipt me-2"></i> 我的订单
                    </a>
                    <a href="/user/favorites">
                        <i class="bi bi-heart me-2"></i> 我的收藏
                    </a>
                    <a href="/user/downloads">
                        <i class="bi bi-download me-2"></i> 我的下载
                    </a>
                </nav>
            </div>
            
            <div class="user-content">
                <h1 class="page-title">我的订单</h1>
                
                <?php if (empty($orders)): ?>
                    <div class="empty-state">
                        <i class="bi bi-inbox" style="font-size: 4rem; color: var(--color-gray-300);"></i>
                        <p>暂无订单记录</p>
                    </div>
                <?php else: ?>
                    <div class="orders-list">
                        <?php foreach ($orders as $order): ?>
                            <div class="order-card">
                                <img src="/uploads/cover/<?= e($order['cover_image'] ?: 'default.jpg') ?>" alt="<?= e($order['title']) ?>">
                                <div class="order-details">
                                    <h3><?= e($order['title']) ?></h3>
                                    <p class="order-info">订单号：<?= e($order['order_no']) ?></p>
                                    <p class="order-info">金额：<span class="price"><?= formatPrice($order['price']) ?></span></p>
                                    <p class="order-info">下单时间：<?= timeAgo($order['create_time']) ?></p>
                                </div>
                                <div class="order-status">
                                    <span class="status-badge status-<?= getOrderStatusBadge($order['status']) ?>">
                                        <?= getOrderStatusText($order['status']) ?>
                                    </span>
                                    <?php if ($order['status'] === 'pending'): ?>
                                        <a href="/payment/pay/<?= $order['id'] ?>" class="btn btn-success btn-sm">
                                            <i class="bi bi-credit-card me-1"></i> 立即支付
                                        </a>
                                    <?php elseif ($order['status'] === 'paid'): ?>
                                        <a href="/download/<?= $order['product_id'] ?>" class="btn btn-primary btn-sm">
                                            <i class="bi bi-download me-1"></i> 下载
                                        </a>
                                    <?php endif; ?>
                                </div>
                            </div>
                        <?php endforeach; ?>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</section>

<style>
.user-center-section {
    padding: 80px 0;
    background: var(--color-gray-50);
    min-height: calc(100vh - 80px);
}

.user-layout {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 2rem;
}

.user-sidebar {
    position: sticky;
    top: 100px;
    height: fit-content;
}

.user-info {
    text-align: center;
    padding: 2rem;
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    margin-bottom: 1.5rem;
}

.user-avatar-circle {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    margin: 0 auto 1rem;
    background: var(--color-primary-gradient);
    display: flex;
    align-items: center;
    justify-content: center;
}

.user-info h3 {
    margin-bottom: 0.5rem;
    color: var(--color-gray-900);
}

.user-info p {
    margin: 0;
    color: var(--color-gray-500);
}

.user-nav {
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    padding: 0.5rem;
}

.user-nav a {
    display: flex;
    align-items: center;
    padding: 1rem 1.5rem;
    color: var(--color-gray-600);
    border-radius: var(--radius-lg);
    transition: all var(--transition-fast);
    text-decoration: none;
}

.user-nav a:hover, .user-nav a.active {
    background: var(--color-gray-50);
    color: var(--color-primary);
}

.user-nav a.active {
    background: var(--color-gray-50);
    color: var(--color-primary);
    font-weight: 600;
}

.user-content {
    background: var(--color-white);
    padding: 2.5rem;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
}

.page-title {
    margin-bottom: 2rem;
    color: var(--color-gray-900);
    font-weight: 700;
}

.empty-state {
    text-align: center;
    padding: 4rem 2rem;
    color: var(--color-gray-500);
}

.orders-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.order-card {
    display: flex;
    gap: 1.5rem;
    padding: 1.5rem;
    background: var(--color-gray-50);
    border-radius: var(--radius-lg);
    align-items: center;
}

.order-card img {
    width: 120px;
    height: 120px;
    object-fit: cover;
    border-radius: var(--radius-md);
}

.order-details {
    flex: 1;
}

.order-details h3 {
    margin-bottom: 0.75rem;
    color: var(--color-gray-900);
    font-weight: 600;
}

.order-info {
    margin: 0.25rem 0;
    color: var(--color-gray-600);
}

.order-info .price {
    color: var(--color-primary);
    font-weight: 700;
    font-size: 1.1rem;
}

.order-status {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 1rem;
}

.status-badge {
    padding: 0.5rem 1rem;
    border-radius: var(--radius-md);
    font-size: 0.9rem;
    font-weight: 600;
}

.status-success {
    background: #d4edda;
    color: #155724;
}

.status-warning {
    background: #fff3cd;
    color: #856404;
}

.status-secondary {
    background: #f8f9fa;
    color: #6c757d;
}

.status-danger {
    background: #f8d7da;
    color: #721c24;
}

.btn-sm {
    padding: 0.5rem 1rem;
    font-size: 0.9rem;
}

@media (max-width: 768px) {
    .user-layout {
        grid-template-columns: 1fr;
    }
    
    .order-card {
        flex-direction: column;
        text-align: center;
    }
    
    .order-card img {
        width: 100%;
        max-width: 200px;
        height: 200px;
    }
    
    .order-status {
        align-items: center;
    }
}
</style>

<?php require __DIR__ . '/../_footer.phtml'; ?>
