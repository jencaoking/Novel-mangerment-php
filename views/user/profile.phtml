<?php require __DIR__ . '/../_header.phtml'; ?>

<div class="container mt-5 mb-5">
    <div class="row">
        <div class="col-md-3 mb-4">
            <div class="list-group shadow-sm">
                <a href="/user" class="list-group-item list-group-item-action">用户中心</a>
                <a href="/user/profile" class="list-group-item list-group-item-action active">个人资料设置</a>
                <a href="/user/orders" class="list-group-item list-group-item-action">我的订单</a>
                <a href="/user/downloads" class="list-group-item list-group-item-action">我的下载</a>
            </div>
        </div>

        <div class="col-md-9">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-white border-bottom">
                    <h5 class="mb-0 py-2">个人资料设置</h5>
                </div>
                <div class="card-body p-4">
                    <?php if (!empty($error)): ?>
                        <div class="alert alert-danger" role="alert">
                            <i class="bi bi-exclamation-triangle-fill me-2"></i> <?= htmlspecialchars($error) ?>
                        </div>
                    <?php endif; ?>
                    
                    <?php if (isset($success) && $success): ?>
                        <div class="alert alert-success" role="alert">
                            <i class="bi bi-check-circle-fill me-2"></i> 资料更新成功！
                        </div>
                    <?php endif; ?>

                    <form action="/user/profile" method="POST" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" value="<?= htmlspecialchars(generateCSRFToken()) ?>">
                        
                        <div class="text-center mb-4">
                            <div class="position-relative d-inline-block">
                                <?php if (!empty($user['avatar'])): ?>
                                    <img src="/uploads/avatar/<?= htmlspecialchars($user['avatar']) ?>" alt="Avatar" class="rounded-circle border" style="width: 120px; height: 120px; object-fit: cover;">
                                <?php else: ?>
                                    <div class="rounded-circle bg-secondary d-flex align-items-center justify-content-center text-white mx-auto border" style="width: 120px; height: 120px; font-size: 3rem;">
                                        <i class="bi bi-person"></i>
                                    </div>
                                <?php endif; ?>
                            </div>
                            <div class="mt-3 w-50 mx-auto">
                                <label for="avatar" class="form-label text-muted small">支持 JPG/PNG/GIF 格式，最大 2MB</label>
                                <input class="form-control form-control-sm" id="avatar" name="avatar" type="file" accept="image/jpeg,image/png,image/gif">
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="username" class="form-label fw-bold">用户名</label>
                            <input type="text" class="form-control" id="username" name="username" value="<?= htmlspecialchars($user['username'] ?? '') ?>" required>
                        </div>
                        
                        <div class="mb-4">
                            <label for="email" class="form-label fw-bold">邮箱地址</label>
                            <input type="email" class="form-control" id="email" name="email" value="<?= htmlspecialchars($user['email'] ?? '') ?>" required>
                        </div>
                        
                        <hr class="mb-4">
                        
                        <div class="text-end">
                            <button type="submit" class="btn btn-primary px-4 py-2">
                                <i class="bi bi-save me-1"></i> 保存修改
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<?php require __DIR__ . '/../_footer.phtml'; ?>