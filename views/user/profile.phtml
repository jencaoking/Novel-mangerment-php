<?php $pageTitle = '个人资料设置'; ?>
<?php require __DIR__ . '/../_header.phtml'; ?>

<section class="user-center-section">
    <div class="container">
        <div class="user-layout">
            <div class="user-sidebar">
                <div class="user-info">
                    <div class="user-avatar-circle">
                        <i class="bi bi-person text-white" style="font-size: 2rem;"></i>
                    </div>
                    <h3><?= e($user['username']) ?></h3>
                    <p><?= e($user['email']) ?></p>
                </div>
                <nav class="user-nav">
                    <a href="/user">
                        <i class="bi bi-speedometer2 me-2"></i> 概览
                    </a>
                    <a href="/user/profile" class="active">
                        <i class="bi bi-person-gear me-2"></i> 个人资料
                    </a>
                    <a href="/user/orders">
                        <i class="bi bi-receipt me-2"></i> 我的订单
                    </a>
                    <a href="/user/downloads">
                        <i class="bi bi-download me-2"></i> 我的下载
                    </a>
                </nav>
            </div>
            
            <div class="user-content">
                <h1 class="page-title">个人资料设置</h1>
                
                <!-- 显示消息 -->
                <?php if (isset($error) && $error): ?>
                    <div class="alert alert-danger d-flex align-items-center gap-2 mb-4" role="alert">
                        <i class="bi bi-exclamation-circle-fill"></i>
                        <span><?= e($error) ?></span>
                    </div>
                <?php endif; ?>

                <?php if (isset($success) && $success): ?>
                    <div class="alert alert-success d-flex align-items-center gap-2 mb-4" role="alert">
                        <i class="bi bi-check-circle-fill"></i>
                        <span><?= e($success) ?></span>
                    </div>
                <?php endif; ?>

                <?php
                // 强制改密横幅: 当 must_change_password=1 时显示
                $mustChangePwd = !empty($user['must_change_password']);
                ?>
                <?php if ($mustChangePwd): ?>
                    <div class="alert alert-warning d-flex align-items-start gap-2 mb-4" role="alert">
                        <i class="bi bi-shield-exclamation" style="font-size: 1.5rem;"></i>
                        <div>
                            <strong>您必须修改密码后才能使用其他功能</strong>
                            <div class="small mt-1">系统检测到您的账户使用默认或临时密码, 请在下方"修改密码"卡片中设置新密码。完成后该提示会自动消失。</div>
                        </div>
                    </div>
                <?php endif; ?>

                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-header bg-white border-bottom">
                        <h5 class="mb-0 py-2"><i class="bi bi-person me-2"></i>个人资料</h5>
                    </div>
                    <div class="card-body p-4">
                        <form action="/user/profile" method="POST" enctype="multipart/form-data">
                            <input type="hidden" name="csrf_token" value="<?= htmlspecialchars(generateCSRFToken()) ?>">
                            
                            <div class="text-center mb-4">
                                <div class="position-relative d-inline-block">
                                    <?php if (!empty($user['avatar'])): ?>
                                        <img src="/uploads/avatar/<?= htmlspecialchars($user['avatar']) ?>" alt="Avatar" class="rounded-circle border" style="width: 120px; height: 120px; object-fit: cover;">
                                    <?php else: ?>
                                        <div class="rounded-circle bg-gradient d-flex align-items-center justify-content-center text-white mx-auto border" style="width: 120px; height: 120px; font-size: 3rem; background: var(--color-primary-gradient);">
                                            <i class="bi bi-person"></i>
                                        </div>
                                    <?php endif; ?>
                                </div>
                                <div class="mt-3 w-75 mx-auto">
                                    <label for="avatar" class="form-label text-muted small">支持 JPG/PNG/GIF 格式，最大 2MB</label>
                                    <input class="form-control form-control-sm" id="avatar" name="avatar" type="file" accept="image/jpeg,image/png,image/gif">
                                </div>
                            </div>

                            <div class="mb-3">
                                <label for="username" class="form-label fw-bold">用户名</label>
                                <input type="text" class="form-control" id="username" name="username" value="<?= htmlspecialchars($user['username'] ?? '') ?>" required>
                            </div>
                            
                            <div class="mb-4">
                                <label for="email" class="form-label fw-bold">邮箱地址</label>
                                <input type="email" class="form-control" id="email" name="email" value="<?= htmlspecialchars($user['email'] ?? '') ?>" required>
                            </div>
                            
                            <div class="text-end">
                                <button type="submit" class="btn btn-primary px-4 py-2">
                                    <i class="bi bi-save me-1"></i> 保存修改
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <div class="card shadow-sm border-0" id="change-password">
                    <div class="card-header bg-white border-bottom text-danger">
                        <h5 class="mb-0 py-2"><i class="bi bi-shield-lock me-1"></i> 修改密码</h5>
                    </div>
                    <div class="card-body p-4">
                        <form action="/user/change-password" method="POST" autocomplete="off">
                            <input type="hidden" name="csrf_token" value="<?= htmlspecialchars(generateCSRFToken()) ?>">

                            <?php if (empty($user['must_change_password'])): ?>
                            <div class="mb-3">
                                <label for="old_password" class="form-label fw-bold">原密码</label>
                                <input type="password" class="form-control" id="old_password" name="old_password" required placeholder="请输入当前使用的密码">
                            </div>
                            <?php else: ?>
                            <div class="alert alert-info py-2 small mb-3" role="alert">
                                <i class="bi bi-info-circle me-1"></i>
                                您正在使用初始密码, 无需填写"原密码", 直接设置新密码即可。
                            </div>
                            <?php endif; ?>

                            <div class="row mb-4">
                                <div class="col-md-6">
                                    <label for="new_password" class="form-label fw-bold">新密码</label>
                                    <input type="password" class="form-control" id="new_password" name="new_password" required minlength="6" placeholder="至少6位字符">
                                </div>
                                <div class="col-md-6">
                                    <label for="confirm_password" class="form-label fw-bold">确认新密码</label>
                                    <input type="password" class="form-control" id="confirm_password" name="confirm_password" required minlength="6" placeholder="请再次输入新密码">
                                </div>
                            </div>

                            <div class="text-end">
                                <button type="submit" class="btn btn-outline-danger px-4 py-2">
                                    <i class="bi bi-key me-1"></i> 更新密码
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
.user-center-section {
    padding: 80px 0;
    background: var(--color-gray-50);
    min-height: calc(100vh - 80px);
}

.user-layout {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 2rem;
}

.user-sidebar {
    position: sticky;
    top: 100px;
    height: fit-content;
}

.user-info {
    text-align: center;
    padding: 2rem;
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    margin-bottom: 1.5rem;
}

.user-avatar-circle {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    margin: 0 auto 1rem;
    background: var(--color-primary-gradient);
    display: flex;
    align-items: center;
    justify-content: center;
}

.user-info h3 {
    margin-bottom: 0.5rem;
    color: var(--color-gray-900);
}

.user-info p {
    margin: 0;
    color: var(--color-gray-500);
}

.user-nav {
    background: var(--color-white);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
    padding: 0.5rem;
}

.user-nav a {
    display: flex;
    align-items: center;
    padding: 1rem 1.5rem;
    color: var(--color-gray-600);
    border-radius: var(--radius-lg);
    transition: all var(--transition-fast);
    text-decoration: none;
}

.user-nav a:hover, .user-nav a.active {
    background: var(--color-gray-50);
    color: var(--color-primary);
}

.user-nav a.active {
    background: var(--color-gray-50);
    color: var(--color-primary);
    font-weight: 600;
}

.user-content {
    background: var(--color-white);
    padding: 2.5rem;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-md);
}

.page-title {
    margin-bottom: 2rem;
    color: var(--color-gray-900);
    font-weight: 700;
}

@media (max-width: 768px) {
    .user-layout {
        grid-template-columns: 1fr;
    }
}
</style>

<?php require __DIR__ . '/../_footer.phtml'; ?>
